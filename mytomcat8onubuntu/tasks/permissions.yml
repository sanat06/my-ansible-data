---
- name: give the  tomcat group ownership
  file: 
     path: /opt/tomcat
     state: directory
     recurse: yes
     group: tomcat


- name: give the tomcat read access to the conf directory
  file:
     path: /opt/tomcat/conf
     state: directory 
     rescurse: yes
     mode: g+r


      
- name: give the tomcat execute access into the conf directory
  file:
     path: /opt/tomcat/conf
     state: directory 
     mode: g+x
     
- name: make the tomcat user the owner of the webapps
  file:
     path: /opt/tomcat/webapps/
     state: directory 
     rescurse: yes
     owner: tomcat
     

- name: make the tomcat user the owner of the works
  file:
    path: /opt/tomcat/works/
    state: directory 
    rescurse: yes
    owner: tomcat
    
  
- name: make the tomcat user the owner of the temp
  file:
    path: /opt/tomcat/temp/
    state: directory 
    rescurse: yes
    owner: tomcat
   
  
- name: make the tomcat user the owner of the logs 
  file:
    path: /opt/tomcat/logs/
    state: directory 
    rescurse: yes
    owner: tomcat
    
      
     
     
  
     
